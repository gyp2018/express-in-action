mixin user-widget(user)
  .user-widget
    img(src=user.profilePicture)
    .user-name= user.name
    .user-bio= user.bio

doctype html
html(lang="en")
  head
    title Hello world!
  body
    h1 This is a Pug example
    #container
      p Wow
      +user-widget(currentUser)
